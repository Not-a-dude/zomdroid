name: Build APK

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Decode Keystore File
        run: |
          echo "${{ secrets.KEYSTORE_B64 }}" | base64 --decode > app/release.jks

      - name: Generate local.properties
        env:
          SEC_STORE_PASS: ${{ secrets.KEYSTORE_PASS }}
          SEC_KEY_ALIAS: ${{ secrets.KEYSTORE_KEY_ALIAS }}
          SEC_KEY_PASS: ${{ secrets.KEYSTORE_KEY_PASS }}
        run: |
          echo "ZOMDROID_STORE_FILE=release.jks" >> local.properties
          printf 'ZOMDROID_STORE_PASSWORD=%s\n' "$SEC_STORE_PASS" >> local.properties
          printf 'ZOMDROID_KEY_ALIAS=%s\n' "$SEC_KEY_ALIAS" >> local.properties
          printf 'ZOMDROID_KEY_PASSWORD=%s\n' "$SEC_KEY_PASS" >> local.properties

      - name: Grant Execute Permission for Gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug
        env:
          GRADLE_OPTS: "-Dorg.gradle.workers.max=2"

      - name: Build Release APK
        run: ./gradlew assembleRelease --stacktrace
        env:
          GRADLE_OPTS: "-Dorg.gradle.workers.max=2"

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: zomdroid-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: zomdroid-release
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error